<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Skullhack Store" %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Skullhack Store - Browse products, categories, and manage your orders.">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body class="d-flex flex-column min-vh-100">
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4" aria-label="Main navigation">
        <div class="container-fluid">
          <%= link_to "Skullhack Store", root_path, class: "navbar-brand" %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar"
                  aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item"><%= link_to "Products", products_path, class: "nav-link #{'active' if current_page?(products_path)}" %></li>
              <li class="nav-item"><%= link_to "Categories", categories_path, class: "nav-link #{'active' if current_page?(categories_path)}" %></li>
              <li class="nav-item"><%= link_to "About", about_path, class: "nav-link #{'active' if current_page?(about_path)}" %></li>
              <li class="nav-item"><%= link_to "Orders", orders_path, class: "nav-link #{'active' if current_page?(orders_path)}" %></li>
              <li class="nav-item"><%= link_to "Cart", cart_path, class: "nav-link #{'active' if current_page?(cart_path)}" %></li>
              <% if user_signed_in? && current_user.admin? %>
                <li class="nav-item"><%= link_to "Admin", admin_root_path, class: "nav-link #{'active' if current_page?(admin_root_path)}" %></li>
              <% end %>
            </ul>

            <!-- Search form -->
            <%= form_with url: products_path, method: :get, local: true, class: "d-flex", role: "search" do |f| %>
              <%= f.text_field :search, value: params[:search], placeholder: "Search products...",
                                class: "form-control me-2", aria: { label: "Search products" } %>
              <%= select_tag :category_id,
                  options_from_collection_for_select(Category.order(:name), :id, :name, params[:category_id]),
                  include_blank: "All categories",
                  class: "form-select me-2", aria: { label: "Filter by category" } %>
              <%= f.submit "Search", class: "btn btn-outline-success" %>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    <div class="container mt-3">
      <%= render "shared/flash" %>
    </div>

    <main class="container mb-5 flex-grow-1" role="main">
      <%= yield %>
    </main>

    <footer class="bg-light py-3 border-top mt-auto">
      <div class="container text-center text-muted">
        &copy; <%= Time.current.year %> Skullhack Store · Built with Rails & Bootstrap
      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
